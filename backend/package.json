{
  "name": "smartwallet-backend",
  "version": "1.0.0",
  "description": "SmartWallet API - Personal Finance Tracker Backend",
  "main": "index.js",
  "scripts": {
    "start": "prisma migrate deploy && node index.js",
    "start:dev": "node index.js",
    "dev": "node --watch index.js",
    "postinstall": "prisma generate",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:seed": "node prisma/seed.js",
    "setup": "npm install && npm run db:generate && npm run db:push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["finance", "expense-tracker", "pern-stack", "prisma", "express"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^4.16.2",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "prisma": "^4.16.2"
  }
}
